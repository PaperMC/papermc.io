<!doctype html>
{% extends 'partial/layout.twig' %}
{% set page = 'legacy' %}
{% set title = 'Legacy Downloads – PaperMC' %}
{% block styles %}
  <link type="text/css" rel="stylesheet" href="{{ urls['css/legacy.css'] }}" media="screen, projection"/>
{% endblock %}
{% block content %}
  {# Main Scrollable Content #}
  <main class="container">
    <h1>Legacy Downloads</h1>
    <p><strong>These are here purely for informational reasons.</strong></p>
    <ul>
      <li>
        <i class="tiny material-icons">error</i>
          You will receive <strong>no support</strong> if you decide to use these builds.
      </li>
      <li>
          <i class="tiny material-icons">error</i>
          Any problems you find in these builds <strong>will not be fixed</strong>.
      </li>
      <li>
          <i class="tiny material-icons">error</i>
          Any issues you report for these builds <strong>will be closed</strong>.
      </li>
      <li>
        <i class="tiny material-icons">error</i>
          You may be <strong>kicked or muted</strong> when asking for support on our Discord.
    </li>
    </ul>

    <div class="row" id="quiz">
      <p><strong>
        Please answer the following questions to gain access to the downloads
      </strong></p>
      <form>
        <div class="row">
          <div class="col s6">
            <label for="quiz-1">Will you receive support for these builds?</label>
            <select id="quiz-1" class="browser-default">
              <option value="" disabled selected>Choose your option</option>
              <option value="1">Yes</option>
              <option value="2">No</option>
            </select>
          </div>
        </div>
        <div class="row">
          <div class="col s6">
            <label for="quiz-2">Will issues in these builds be fixed?</label>
            <select id="quiz-2" class="browser-default">
              <option value="" disabled selected>Choose your option</option>
              <option value="1">Yes</option>
              <option value="2">No</option>
            </select>
          </div>
        </div>
        <div class="row">
          <div class="col s6">
            <label for="quiz-3">What will happen if you open an issue for a problem in any of these builds?</label>
            <select id="quiz-3" class="browser-default">
              <option value="" disabled selected>Choose your option</option>
              <option value="1">It will be answered and resolved</option>
              <option value="2">It will be closed</option>
            </select>
          </div>
        </div>
        <div class="row">
          <div class="col s6">
            <label for="quiz-4">What will happen if you request support for these builds on Discord?</label>
            <select id="quiz-4" class="browser-default">
              <option value="" disabled selected>Choose your option</option>
              <option value="1">I will be answered</option>
              <option value="2">I will be kicked</option>
              <option value="3">I will be muted</option>
              <option value="4">I may be kicked or muted</option>
            </select>
          </div>
        </div>
      </form>
      <div class="col s6">
        <button class="btn waves-effect waves-light light-blue darken-2" id="submit-quiz">
          Submit
        </button>
      </div>
    </div>

    <div id="content" class="row"></div>
  </main>
{% endblock %}
{% block footer %}
  <script src="{{ urls['js/legacy.js'] }}" charset="utf-8"></script>
{% endblock %}
